_schema-version: 3.3.0
ID: CommonwealthofPennsylvania
description: A simple CAP project.
version: 1.0.0
modules:
- name: CommonwealthofPennsylvania-srv
  type: nodejs
  path: gen/srv
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: CommonwealthofPennsylvania-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CommonwealthofPennsylvania-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - copa.zip
      name: copa
      target-path: resources/
- name: copa
  type: html5
  path: app/copa
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CommonwealthofPennsylvania-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CommonwealthofPennsylvania-html5-service
    service-plan: app-host
- name: CommonwealthofPennsylvania-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: CommonwealthofPennsylvania-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
